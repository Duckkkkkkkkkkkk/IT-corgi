package com.example.it_corgi.model;

import java.util.Set;
import java.util.HashSet;

public class Order {

    public static Set<Integer> items_id = new HashSet<>(); // выбираем set, чтобы в корзину добавлялся один и тот же курс, только один раз

}
